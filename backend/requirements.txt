# ──────────────────────────────────────────────────────────────
# Dependencias core del backend UPC ABET (producción).
# Versiones pinneadas (==) para garantizar reproducibilidad y
# evitar incompatibilidades en despliegue.
#
# Instalar con:  pip install -r requirements.txt
#
# Las dependencias del RAG (torch, sentence-transformers,
# langchain-*, chromadb, rank-bm25) y de tools (markitdown, pyyaml)
# son OPCIONALES y están en requirements-dev.txt.
# El backend funciona sin ellas (caen a knowledge clásico).
# ──────────────────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
python-multipart==0.0.20
PyPDF2==3.0.1
docx2txt==0.8
python-dotenv==1.0.1
pydantic==2.10.4
